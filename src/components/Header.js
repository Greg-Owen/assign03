import React from 'react'
import PropTypes from 'prop-types'


const Header = (props) => {
  return (
      <header>
        <h1>Assignment 3: My Personal Website</h1>
      </header>
  )
}

// Header.defaultProps ={
//     title: 'N/A',
// }

// Header.propTypes = {
//     title: PropTypes.string.isRequired,
// }

export default Header
